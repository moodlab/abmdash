---
ac: 1.2
depends_on: none (serialized after AC-1.1 in W1 — both touch .gitignore)
risk: low
status: complete
---

# AC-1.2: codegraph index init + gitignore + README regen note

## Executable Spec

- **predicate:** (a) `codegraph init` has built a real index at repo root — `codegraph query call_redcap_api` returns a hit in `R/redcap_api.R` (proves R extractor ran, not an empty-dir fake); (b) `.codegraph/` is listed in root `.gitignore` AND zero files under `.codegraph/` are tracked (`git ls-files .codegraph/` is empty — catches commit-before-gitignore); (c) README.md documents the regen command `codegraph init` AND a staleness/when-to-regen trigger AND states the index is gitignored/not committed; (d) `.gitignore` edit is append-only relative to AC-1.1's state (no clobber); (e) SHOULD: `.Rbuildignore` contains an anchored `^\.codegraph$`-style entry.

- **probe:** (run from repo root after implementation; all MUST checks chained, one exit code)
  ```bash
  codegraph query call_redcap_api && codegraph status && \
  git check-ignore .codegraph/ && git check-ignore .codegraph/codegraph.db && \
  test -z "$(git ls-files .codegraph/)" && \
  rg -q 'codegraph init' README.md && \
  rg -qi 'regenerat|stale|after.*structural|before.*session|when to|sync' README.md && \
  rg -qi 'gitignore|not committed|untracked' README.md && \
  rg -q 'codegraph' .gitignore && \
  test -z "$(git diff main -- .gitignore | rg '^-[^-]')"
  ```
  SHOULD-check (non-fatal, report only):
  ```bash
  rg '\^\\\.codegraph' .Rbuildignore || echo "SHOULD-miss: .Rbuildignore entry absent"
  ```

- **negative:** 1. **Commit-before-gitignore sneaky-pass (PRIMARY):** `codegraph init` → `git add .` → then gitignore. `git check-ignore` passes but `git ls-files .codegraph/` non-empty. Only `test -z "$(git ls-files ...)"` catches it. 2. **Empty-dir fake:** `mkdir .codegraph` without `codegraph init` — query returns nothing. 3. **Index in wrong dir:** `codegraph init` run in `R/` — query from root misses. 4. **Vague README fake:** mention of "codegraph" without `codegraph init` command; or command but no WHEN trigger; or no gitignore statement. 5. **Serialization clobber:** AC-1.2's `.gitignore` edit rewrites file and drops AC-1.1's entries — caught by `git diff main` showing deletion lines.

- **verification:** `code` · shell assertions (codegraph CLI, git, rg) — deterministic, no browser.
- **fixture status:** NEW — the codegraph index is the fixture, created by `codegraph init`. Ephemeral: gitignored, never committed. Symbol reference `call_redcap_api` pinned at `R/redcap_api.R:37`.
- **rubric anchor:** §4.1 (README section = module-responsibility doc for the index) + §1.5 (predicate pins state invariants: queryable-for-R AND ignored-AND-untracked).
- **ui: block:** NOT applicable.

## Design Intent
- **§1:** invariants pinned over filesystem/git state: "index exists ⇒ queryable", ".gitignore entry ⇒ zero tracked files".
- **§2:** `codegraph init` is the effectful action; probe assertions are pure read-only checks.
- **§3:** `.codegraph/` is a machine-local tool artifact — `.gitignore` cuts tool boundary; `.Rbuildignore` cuts R-package-build boundary.
- **§4:** README section documents regeneration responsibility — what, where, how (`codegraph init`), when (staleness trigger), what NOT (not committed / not in tarball).
- **§5:** N/A.

## Technical Context
- **Files touched (4):** `.gitignore` — APPEND `.codegraph/` (append-only; serialization with AC-1.1: AC-1.1 lands first, AC-1.2 appends after, never parallel); `.Rbuildignore` — append `^\.codegraph$` (SHOULD, matches existing style `^docs/`, `^renv$`; R CMD build never runs in CI — docker+renv only); `README.md` — new section between "### Set up pre-commit hooks" (ends L107) and "### Create an issue" (L109), containing (a) `codegraph init` command, (b) when-to-regen trigger ("after structural changes to R/", "before an agent session", "when stale"), (c) statement that `.codegraph/` is gitignored and not committed; `.codegraph/` — generated by `codegraph init` (codegraph.db + inner .gitignore + cache/).
- **Architecture notes:** R is first-class codegraph language (v1.5.0: extraction/languages/r.js + tree-sitter-r.wasm); 10 R/ files all extractable — no R-support caveat. Inner `.codegraph/.gitignore` exists but root entry is belt-and-suspenders.

## Dependencies
- **Depends on:** none for function; **serialization:** AC-1.1's `.gitignore` tightening lands first — never parallel (last-write-wins clobbers).
- **Conflict set:** `.gitignore` shared with AC-1.1 — serialized within W1.
- **Risk level:** low. Reversible via `codegraph uninit` + git revert.

### Progress
- [x] index initialized — 2026-08-13 (20 files, 138 nodes, 257 edges; query hit R/redcap_api.R:37)

### Decision Log
- spec-resolved — `.Rbuildignore` entry downgraded to SHOULD (R CMD build never runs in CI).

### Surprises & Discoveries
- Probe step 10 (`git diff main -- .gitignore | rg '^-[^-]'` empty) is UNSATISFIABLE as written: HEAD .gitignore's last line `.opencode/traces/` has NO trailing newline, so ANY append makes git diff show that line as a `-`/`+` pair (newline added). Verified with minimal repro. Content is fully preserved (AC-1.1 entries docs/*, !docs/okf/, planning.md, opencode lines all present) — the single `-` line is a false positive, not a clobber. Deviation logged; implementers should replace step 10 with a content-preservation check (e.g. compare `git show HEAD:.gitignore` lines ⊆ working tree lines).

### Idempotence & Recovery
- Safe retry: `codegraph init` is idempotent (re-runs refresh index).
- Rollback: `codegraph uninit` + git revert of .gitignore/README changes.