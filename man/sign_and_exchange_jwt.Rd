% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gcal_api.R, R/gsheet_api.R
\name{sign_and_exchange_jwt}
\alias{sign_and_exchange_jwt}
\title{Sign and Exchange JWT}
\usage{
sign_and_exchange_jwt(service_account, scope)

sign_and_exchange_jwt(service_account, scope)
}
\arguments{
\item{service_account}{List containing parsed service account JSON.}

\item{scope}{Character scalar OAuth2 scope to request.}
}
\value{
Character string containing the access token.

Character string containing the access token.
}
\description{
Internal function to build the JWT claim for a service account, sign it with
the private key, and exchange it for an access token at the OAuth2 token
endpoint.

Internal function to build the JWT claim for a service account, sign it with
the private key, and exchange it for an access token at the OAuth2 token
endpoint.
}
\details{
This is the gcal module's own copy — gsheet_api.R keeps a separate copy so
the modules stay independent (merging them breaks the wrong-mock tripwire in
the behavior-lock tests).
}
\keyword{internal}
