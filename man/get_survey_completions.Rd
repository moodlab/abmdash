% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redcap_api.R
\name{get_survey_completions}
\alias{get_survey_completions}
\title{Get Survey Completion Status}
\usage{
get_survey_completions(surveys = NULL, records = NULL, format = "json")
}
\arguments{
\item{surveys}{Character vector of survey instrument names to check. If NULL, all surveys are included.}

\item{records}{Character vector of record IDs to check. If NULL, all records are included.}

\item{format}{Character string specifying the format ("json", "csv"). Default is "json"}
}
\value{
Data frame with columns: record_id, survey_instrument, survey_timestamp, survey_complete
}
\description{
Retrieves survey completion timestamps and status for all participants.
}
\examples{
\dontrun{
# Get all survey completions
completions <- get_survey_completions()

# Get specific surveys
baseline_completions <- get_survey_completions(surveys = c("baseline_survey", "followup_survey"))

# Get completions for specific participants
participant_completions <- get_survey_completions(records = c("001", "002", "003"))
}
}
